---
- hosts: all
  gather_facts: False
  roles:
  - ansible-local-ssh-known-hosts-role
  tasks:
  - name: assert the hosts are in the known_hosts
    when: inventory_hostname != 'lxc-test'
    assert:
      that: "q('lines', 'ssh-keygen -F ' +  ansible_ssh_host | d(inventory_hostname, True))"
